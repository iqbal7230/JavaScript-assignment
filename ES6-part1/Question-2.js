console.log([...'Lydia']);

// Output:
// [ 'L', 'y', 'd', 'i', 'a' ]

/* Explanation:
The spread operator (...) used to expand iterable objects (like strings, arrays, etc.) into individual elements.
*/
