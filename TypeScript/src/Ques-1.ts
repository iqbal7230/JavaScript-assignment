1. /*Define an interface User with the following properties: 
        // id(number), name(string), email(string), age(number optional) */

interface User{
    id: number;
    name:string;
    email:string;
    age?:number ;
}
